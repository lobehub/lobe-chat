'use client';

import { PropsWithChildren, memo } from 'react';

const Footer = memo<PropsWithChildren>(() => {
  // Footer functionality removed
  return null;
});

Footer.displayName = 'SettingFooter';

export default Footer;
