import { Center } from 'react-layout-kit';

import LobeChatText from './LobeChatText';

export default () => {
  return (
    <Center height={'100%'} width={'100%'}>
      <LobeChatText />
    </Center>
  );
};
