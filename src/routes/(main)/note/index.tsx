'use client';

import { memo } from 'react';

import { NotePlaceholder } from '@/features/QuickNote';

const NotePage = memo(() => <NotePlaceholder />);

NotePage.displayName = 'NotePage';

export default NotePage;
