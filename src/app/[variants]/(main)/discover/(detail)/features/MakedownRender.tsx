// Stub file for compatibility
// Discover features were removed in Serenvale Phase 1

interface MakedownRenderProps {
  children: string;
}

const MakedownRender = ({ children }: MakedownRenderProps) => {
  return <div>{children}</div>;
};

export default MakedownRender;
