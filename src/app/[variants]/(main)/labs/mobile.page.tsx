'use client';

import { memo } from 'react';

import LabsPage from './index';

/**
 * Mobile Labs Page
 * Labs page for experimental features
 */
export const MobileLabsPage = memo(() => {
  return <LabsPage />;
});

MobileLabsPage.displayName = 'MobileLabsPage';
