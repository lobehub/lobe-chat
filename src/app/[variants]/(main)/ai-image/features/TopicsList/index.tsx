import { Flexbox } from 'react-layout-kit';

const TopicsList = () => {
  return (
    <Flexbox
      style={{
        height: '100%',
        overflowY: 'auto',
        width: 80,
      }}
    >
      主题列表
    </Flexbox>
  );
};

export default TopicsList;
