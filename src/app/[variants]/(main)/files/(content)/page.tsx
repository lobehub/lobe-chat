/**
 * This page.tsx is required for Next.js to recognize the /files route
 * The actual content is rendered by layout.tsx -> Client.tsx
 * which uses react-router-dom for SPA routing
 */
export default () => {
  return null;
};
