/**
 * Rendered in the composer control bar, next to the approval-mode chip. The
 * open-source build keeps this slot empty; downstream builds can override the
 * module through their own `@/business/...` mapping to surface a remaining
 * balance indicator there.
 */
const ChatInputCredits = () => null;

export default ChatInputCredits;
