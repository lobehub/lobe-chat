/**
 * Business extension point for DevDock items.
 *
 * Registration is intentionally invoked from the lazy DevDock entry so the
 * application has no eager dependency on business-only panels.
 */
export const registerBusinessDevDockItems = () => {};
