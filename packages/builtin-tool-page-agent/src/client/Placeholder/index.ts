import { type BuiltinPlaceholder } from '@lobechat/types';

/**
 * Page Agent Placeholder Components Registry
 *
 * Placeholder components are shown while a tool is executing,
 * before the final result is available.
 */
export const PageAgentPlaceholders: Record<string, BuiltinPlaceholder> = {};
