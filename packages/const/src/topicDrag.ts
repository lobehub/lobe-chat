/**
 * DataTransfer MIME type used when dragging a topic row into the chat input.
 * A custom type avoids Lexical treating the payload as dropped plain text.
 */
export const TOPIC_DRAG_MIME = 'application/x-lobe-topic';
