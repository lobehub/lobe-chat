build:
  steps:
    - eas/build
    - run:
        command: |
          curl \
            -H 'Content-Type: application/json' \
            -d '{"content": "Build ios production succeeded! URL: ${ eas.job.expoBuildUrl }"}' \
            $DISCORD_WEBHOOK_URL
